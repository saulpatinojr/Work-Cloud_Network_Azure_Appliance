# Changelog

All notable changes to the Cloud Network Assessment Azure Appliance are documented in
this file. Application changes are recorded in the core repository's `CHANGELOG.md`; this file
covers deployment, operations and this repository itself.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased]

### Changed
- **Images are deployed by digest** (core `TODO.md` → T-701; v1.0 review findings RELEASE-001 / IMG-012). `230-image-update` now requires the core manifest's `digests{}` and composes the pinned reference `docker.io/<ns>/cna:<role>-sha-<7>@sha256:<digest>` for the api, worker, web and migrator images — the tag stays for people and for the web tier's image-update check, the digest is what the platform pulls — and passes all four to `210` (new `migrator_image` input). A manifest without `digests{}` fails the update with a message to rebuild the core first. `210-deploy` refuses any empty or floating (`*-latest`) reference, drops the `migrator-latest` default and the never-working "resolve from the appliance's own build manifest" step, validates every image input with one shared `validate_image_ref` function (pinned accepted, tag-only accepted with a warning, anything else refused), and records the pinned references in the release catalog. Rollback takes `previous_migrator_image` through `workflow_call`; a dispatch rollback resolves the migrator from the catalog record that matches `previous_api_image` (GitHub caps `workflow_dispatch` at ten inputs). `220-fast-redeploy` applies the same validation; `350`/`360` no longer fall back to floating tags when the catalog has no image (they fail instead); `100-validate-prereqs` checks Docker Hub pull access with an authenticated tags listing rather than by pulling `*-latest`. Terraform image variables lose their `example-namespace/*-latest` module defaults and document the pinned form. Shared change — `230`, `resolve_rollback_target.py`, `validate_image_ref` and the `update-available` issue template are byte-identical in the sibling appliance.

### Security
- **The web tier authenticates to cna-api with a Terraform-generated token** (v1.0 review findings SEC-001 / ARCH-002). Each workload root generates `CNA_API_TOKEN` (`random_password`, 48 characters), the runtime module stores it as the Key Vault secret `cna-api-token`, and both the api and the web Container Apps receive it as a secret reference — the core's API rejects every request without `Authorization: Bearer <token>` (except `/health` and `/ready`) and refuses to start in appliance mode without it. The api ingress was already internal-only on Azure; the token closes the network-trust assumption itself. **Upgrade order:** apply this release with `210` before letting `230` roll the core images that carry the token check.
- **The api readiness and startup probes use `/ready`** (PY-001 / IMG-011), which runs `SELECT 1` against the database; `/health` stays the liveness probe. A revision with a broken database reference no longer receives traffic.
- **Workflow inputs no longer reach the shell unquoted** (DEVOPS-001, DEVOPS-009). Every `${{ inputs.* }}`, `${{ github.event.* }}`, `${{ steps.*.outputs.* }}`, `${{ vars.* }}` and `${{ env.* }}` that was interpolated into a `run:` block in `000`, `100`, `210`, `220`, `230` and `330` is bound through `env:` and referenced as a quoted variable. `230-image-update` validates the dispatch payload before it can start `210`: the namespace shape, `sha_tag`, and every `images.*` against `^docker\.io/<DOCKERHUB_NAMESPACE>/cna:(api|web|worker|migrator)-sha-[0-9a-f]{7}$` with the tag equal to `<role>-<sha_tag>`; its job permissions are the minimum each job needs. Shared workflow — byte-identical in the AWS sibling.
- **Terraform plan files are no longer uploaded as artifacts** (DEVOPS-002). A saved plan stores variable values in plaintext, so the `tfplan-*` artifacts carried the database admin password, the Entra client secret, the NextAuth secret and the credential-encryption key for anyone with read access. The plan text goes to the step summary; the apply job re-plans and still runs the destructive-plan guard before applying. Past artifacts need deleting and the secrets rotating — `REVIEW.md` → R-006.
- **`340-sync-keys` is removed** (DEVOPS-003). It exported every runtime secret into a downloadable `.env` artifact. Key Vault is the read path.
- **Prod-destroying jobs run under the `hub` environment** (DEVOPS-005). `330-teardown`'s `terraform-destroy` and `cleanup-drift` for `prod` wait for `hub`'s required reviewers; `purge-soft-deleted` and `destroy-backend` stay at repository scope for identity reasons and are gated transitively through `needs` — `REVIEW.md` → R-011.

### Fixed
- `100-validate-prereqs` no longer checks the AWS-named secrets `CNA_AWS_ROLE_ARN` and `CNA_PUBLISH_BUCKET` (nothing here uses them; `CLAUDE.md` forbids AWS content), and stale "workflow 211 / 220" wording in `100`, `220`, `330` and `scripts/Initialize-CnaGitHubSecrets.ps1` names the current workflows.

### Changed
- `README.md`: `340` removed from the layout, scope and configuration tables.
- `REVIEW.md` gains R-006 – R-011 (the review's human decisions for this appliance).
- The core's build manifest now also carries `digests{}` (`docker.io/<ns>/cna@sha256:…`) next to `images{}`; `210` still deploys by immutable tag — moving to digests is the core's `TODO.md` → T-701.

### Added
- **The destroy guard also protects the Foundry account, its model deployment and the storage account** (`TODO.md` → T-101). `210`'s apply job now refuses a platform or workload plan that would delete or replace `azurerm_cognitive_account`, `azurerm_cognitive_deployment` or `azurerm_storage_account`, on top of the T-103 log-store and database guard — the failure mode T-101 asks a human to read for (a `moved` block missing on the count-gated AI module shows up as a destroy of `module.ai`) can no longer reach `apply`. T-101's static half is done and recorded: against the core commit dev was last applied from, every count-gated resource has its `moved` block and the resource inventory is otherwise identical, so the expected first plan (six `moved` annotations plus in-place Container App env updates, no create/destroy) is written out in `TODO.md` for the person who runs it. AWS mirror: the guard there also covers `aws_s3_bucket`.
- **`380 · Project Board`** (`TODO.md` → T-102). Adds every newly opened or reopened issue and pull request — `230`'s `update-available` issues especially — to the one GitHub Project shared with the core and the sibling appliance, with `gh project item-add` (no third-party action to pin). Inert by design until the core's `REVIEW.md` R-012 chooses the token: the job is skipped while the `PROJECT_BOARD_URL` variable is unset and exits with a notice while the `PROJECT_BOARD_TOKEN` secret is unset, never failing an issue or PR over a missing board. Shared workflow — identical in the sibling appliance in the same change set.
- **`210` refuses a plan that would destroy the log store or the database** (`TODO.md` → T-103). Right before each `terraform apply`, the apply job runs `terraform show -json tfplan` through the new shared `scripts/ci/refuse_destructive_plan.py`, which fails the run if any `azurerm_log_analytics_workspace` or `azurerm_postgresql_flexible_server` would be deleted or replaced — the exact damage a stale state layout would have done to the workspace, and history no re-apply brings back. The protected types are the step's `PROTECTED_RESOURCE_TYPES`; a human fixes state or knowingly lifts the guard for one run. T-103 itself is closed as verified not applicable: the workspace moved to the platform root on 2026-06-28 and dev was rebuilt from nothing on 2026-08-28 with that layout, so no environment has the old state. Shared script and step mirrored in the AWS appliance in the same change set.
- **`210` verifies the AI Foundry path from inside the environment** (`TODO.md` → T-104). The manifest's `foundry_private_dns_validation` and `foundry_managed_identity_inference` were hardcoded `required` and nothing ever flipped them, so a green run reported `healthy` on a path nobody had exercised. In `saas` mode the apply job now runs a one-off Container Apps Job — same environment, same user-assigned identity, same api image as `cna-api` — executing `scripts/ci/probe_foundry_path.py`: the Foundry host must resolve, from inside the VNet, to an address in the private-endpoint subnet, and one managed-identity chat completion against the deployment the api is configured with must succeed. The verdicts enter the manifest through `update_apply_evidence.py` (new optional `AI_PATH_CHECKS` input), and `evaluate_deployment_evidence.py` now refuses `healthy` — and the catalog entry — while any validation check is not `passed` / `not_applicable`, recording the outstanding checks instead. Shared scripts mirrored in the AWS appliance in the same change set, together with the AWS twin (a Fargate probe for the `bedrock_*` markers).
- **A failing drift check now reaches a person** (`TODO.md` → T-105). `350 · Drift Detection · dev` and `360 · Drift Detection · prod` gain a `report-failure` job that runs when the drift job fails and opens a GitHub issue assigned to the repository owner, deduplicated by exact title so a month of daily failures is one issue that gets staler and louder — each further failure adds a comment — instead of the workflow-author e-mail that a bot-authored schedule sends to nobody. The platform `terraform init` step now classifies the failure: a missing state backend or an empty state is "the dev/prod environment does not exist" (a deleted or never-deployed environment — the five-week silence behind T-105), everything else "the drift check failed"; the two get different issue titles and messages. Drift itself is still a run warning, never an issue. Shared workflow change — mirrored in the AWS appliance in the same change set.
- **`scripts/ci/update_apply_evidence.py` records a cloud-neutral `edge_host_name`** (the Front Door endpoint host here, the CloudFront domain on AWS) beside the existing Front Door keys, plus a handful of AWS keys (`cloudfront_distribution_id`, `ecs_cluster_name`, `alb_dns_name`, `db_address`, `artifacts_bucket`) that are written only when their variables are set — so this appliance's manifests are unchanged apart from the one added key, and `210` here is untouched. Mirror of the AWS appliance's change (its `TODO.md` → T-101, the real AWS `210-deploy`); the script is shared and must stay identical in both repositories.
- **Image-update check contract for the web tier** (core `CHANGELOG.md` → Unreleased, Admin → Updates). Both workload roots inject `CNA_WEB_IMAGE` (the deployed web image reference), `CNA_IMAGE_REGISTRY_USERNAME`, `CNA_APPLIANCE_REPO` (`github_owner/github_repository`) and, as a secret-backed variable, `CNA_IMAGE_REGISTRY_TOKEN` from the Container App secret `container-registry-password` — the same pull-only credential, no new GitHub secret or variable. The web tier uses them to compare its running build with the newest published build in the registry and to link to this repository's `230-image-update` and `210-deploy`. Mirrored in the sibling appliance.
- **Deployment-side backlog transferred from the core** (core `TODO.md` T-507, 2026-09-15). The core removed its copy of the deployment layer (core T-504) and handed over every item that describes this repository's Terraform and workflows: `TODO.md` T-103 (the Log Analytics workspace state-migration runbook), T-104 (Foundry-path validation markers in the deploy manifest), T-105 (drift-check failure visibility) and `REVIEW.md` R-003 (live Azure acceptance sign-off) and R-004 (dev-environment deletion protection). T-106 carries the five Terraform findings the core's production-readiness review engine had recorded for the Azure modules, with paths rebased to `infra/terraform/modules/`. `scripts/validate_documentation_model.py` picks up the core's `.kiro/` exclusion so the two copies stay identical.
- **Initial import from the core repository** (`saulpatinojr/Work-Cloud_Network_Assessment` at `1951830`). The Azure Terraform (`infra/terraform/providers/azure/*` → `infra/terraform/modules/*`, `infra/terraform/environments/azure/{dev,prod}` → `infra/terraform/environments/{dev,prod}`), the deploy and operations workflows (`000`, `100`, `211`/`212` → `210-deploy`, `220`, `320`, `330`, `340`, `350`, `360`), the CI evidence scripts and the release catalog were cut from the core so that customers see one cloud per repository. Module sources were rewritten to the new layout; nothing else in the Terraform changed. `320-publish-portal` lost its `cloud` input — this appliance publishes to Azure Blob Storage only.
- **`230 · Image Update`.** Listens for the core's `cna-image-published` dispatch and polls the core's build manifest every six hours; auto-deploys `dev` through `210` with the recorded `ai_mode`, and opens an `update-available` issue for `prod`. Never touches an environment that has not been deployed once.
- **`300 · Validate`.** This repository's CI: `detect-secrets` (gating), the documentation-model guard, and `terraform fmt` / `terraform validate` on all four roots.
- `README.md` (the intro page), `CLAUDE.md` (agent rules: app code lives in the core; every structural change is mirrored to the sibling appliance), `REVIEW.md`, `TODO.md`, and the `update-available` issue template.
- The dev release catalog (`.deployment-catalog/dev/`) carries over from the core so `230-image-update` and rollback resolution see the environment's real history.

### Fixed
- **`210`'s two `actions/create-github-app-token` steps are SHA-pinned** (`bcd2ba49…` — v3.2.0, the digest `230` and the AWS appliance already use); they were the only actions in the repository still pinned to a tag, against the `CLAUDE.md` rule.
- **The migration connectivity probe never ran.** Its job manifest pulled the migrator image with `DOCKERHUB_USERNAME` / `DOCKERHUB_PASSWORD`, which no step defined, so the (best-effort) probe job failed to create every time and the step fell through silently. The step now defines both from the same variable and secret the migration job itself uses.
- **`350`/`360` fail when the drift plan itself fails.** Both drift workflows ran `terraform plan -detailed-exitcode ... || EXIT=$?` and then tested only for exit 2, so a plan that *errored* (exit 1 — a missing backend, an expired credential, a provider fault) was read as "no drift" and the job went green. That is the failure mode `TODO.md` → T-105 describes from the other side: the check must be loud before its delivery can be. Exit 0 is still clean and exit 2 still a drift warning; any other exit now fails the job with the plan's own exit code, for both the platform and the workload plan. Mirrors the AWS appliance, whose `350`/`360` were written this way (its `CHANGELOG.md` recorded the sibling gap). Shared workflow change — no other repository is affected; the core owns no drift workflow.
- **The workload storage account's firewall window.** The application storage account has been deny-by-default since `TODO.md` T-106, and `azurerm_storage_account_static_website` is provisioned and refreshed over the data plane, but no workflow allow-listed the runner's IP the way the Key Vault steps do — from any runner not already in `ip_rules`, the workload plan/apply and the portal publish fail with `403` (`AuthorizationFailure`). `210-deploy` (plan and apply), `350`/`360-drift` and `320-publish-portal` now add the runner's public IP before touching the account and remove it in an `always()` step, resolving the account from Terraform state (`storage_account_name` output) rather than restating the naming rule. The first apply of an environment has no account to open, so `210` passes the IP as `TF_VAR_deploy_runner_ip` → workload variable `deploy_runner_ip` → storage module `bootstrap_ip_rules`, which the account is created with; the existing `ignore_changes` on `ip_rules` makes that a creation-time value only. Azure-only wiring (Secrets Manager and S3 have no equivalent), so nothing changes in the sibling.

### Changed
- **`TODO.md` → T-106 closed for everything engineering can do** (the Terraform findings imported from the core's review). The MEDIUM storage-account network-rules finding is confirmed applied; the two INFORMATIONAL sweeps re-run clean (`fmt`, `init -backend=false`, `validate` on all four roots) and are enforced by `300` on every push; the HIGH is the product owner's live acceptance (`REVIEW.md` R-003) and the LOW is dev's live `-aif2` state, which `350` now surfaces. Documentation only.
- **The core is now `Work-Cloud_Network_Core`** (core `TODO.md` T-509, 2026-09-18). The application code and the image builds moved from the project's original repository, `Work-Cloud_Network_Assessment`, into a dedicated internal core repository; the original is archived. `CLAUDE.md` and `README.md` point at the new repository and its Wiki, and the `CORE_REPO` variable — the only place `230-image-update` learns where the build manifest lives — must be set to the new name by the repository admin (`REVIEW.md` R-005). Nothing else changes: the image tag scheme, the manifest schema, the `cna-image-published` dispatch and the runtime environment contract are the same, so a deployed environment is unaffected. Mirrored in the sibling appliance in the same change set.
- **Every workflow runs on GitHub-hosted runners (`ubuntu-latest`).** `300 · Validate` moved first (its jobs need no cloud credentials, and a new repository had no runner registered, which left the first pull request's checks queued indefinitely); the deploy and operations workflows — `000`, `100`, `210`, `220`, `230`, `320`, `330`, `340`, `350`, `360` — follow now that the self-hosted runner the original repository used is retired. Nothing in them needs a private network: cloud access is OIDC, the migrator runs as a Container App job inside Azure, and every job that reads Key Vault over the data plane already opens a per-run firewall window for the runner's own public IP (`340 · Sync Local Keys` gains the same window — it read the deny-by-default vault without one). `scripts/bootstrap-runner.sh` (the VPS runner provisioner) and its `.secrets.baseline` entry are removed. Mirrored in the sibling appliance.

